machine:
  node:
    version: 8
dependencies:
  pre:
    - npm install
test:
  override:
    - npm run lint
deployment:
  production:
    branch: master
    commands:
      - echo "Need to set up Up!"
